info:
  name: create_account 2
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/accounts"
  body:
    type: json
    data: |-
      {
        "name": "Emergency Fund"
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("should return 201", function () {
          expect(res.getStatus()).to.equal(201);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
