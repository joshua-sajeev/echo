info:
  name: unarchive_account
  type: http
  seq: 9

http:
  method: PATCH
  url: "{{base_url}}/accounts/3/unarchive"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("should return 204", function () {  
          expect(res.getStatus()).to.equal(204); 
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
