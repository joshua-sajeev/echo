info:
  name: delete_jar
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{base_url}}/jars/1"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("should return 204", function () {
          expect(res.getStatus()).to.equal(204);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
