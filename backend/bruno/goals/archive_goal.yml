info:
  name: archive_goal
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{base_url}}/goals/1/archive"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("should return 204", function () {
          expect(res.getStatus()).to.equal(204);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
