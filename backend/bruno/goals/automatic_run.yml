info:
  name: automatic_run
  type: http
  seq: 10

http:
  method: POST
  url: "{{base_url}}/allocation/distribute"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
