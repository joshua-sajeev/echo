info:
  name: protected_unauthorized
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/accounts"

runtime:
  scripts:
    - type: tests
      code: |-
        test("should return 401", function () {
          expect(res.getStatus()).to.equal(401);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
